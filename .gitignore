# uv virtual environment
.venv/

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# convert.py 的临时中间文件（不提交）
_tmp_convert.urdf
_tmp_convert.xml

# build_urdf.py 的产物（由 xacro 编译生成，不提交）
openflex_integrated_robot.urdf

# 注意: openflex_mujoco.xml 与 mujoco_meshes/ 现已提交到仓库,
#       克隆者无需运行 convert.py 即可直接用 viewer 打开;
#       若需基于最新 packages 重新生成, 见 README 的“重新构建”一节。

# Misc
MUJOCO_LOG.TXT
