[project]
name = "openflex-mujoco"
version = "1.0.0"
description = "OpenFlex v10 双臂机械臂 URDF 转 MuJoCo MJCF 工程（uv 管理依赖）"
readme = "README_CN.md"
requires-python = ">=3.10"
dependencies = [
    "mujoco>=3.6.0",
    "trimesh>=4.11.5",
    "pycollada>=0.9.3",
]

[tool.uv]
package = false
